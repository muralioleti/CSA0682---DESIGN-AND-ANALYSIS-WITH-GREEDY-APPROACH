#include<stdio.h>
int main()
{
	int arr[]={12,45,87,98,80,56,54,23,},i;
	int length=sizeof(arr)/sizeof(arr[0]);
	int max=arr[0];
	for(i=0;i<length;i++)
	{
		if(arr[i]>max)
		   max=arr[i];
	}
	printf("largest element in the array:%d\n",max);
	return 0;
}
